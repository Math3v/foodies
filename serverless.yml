service: aws-foodies

provider:
  name: aws
  runtime: nodejs8.9
  stage: dev
  region: eu-west-2

functions:
  app:
    handler: index.handler
    events:
      - http: ANY /
      - http: 'ANY {proxy+}'
